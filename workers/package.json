{
  "name": "hummbl-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for HUMMBL workflow execution",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.90.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
